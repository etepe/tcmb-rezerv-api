# tqrlab.com Astro repo (UI drop-in) — public env.
# Deploy edilmiş tcmb-rezerv-api Worker URL'i. Yalnız public API tabanı;
# hiçbir anahtar/sır burada DEĞİL (EVDS anahtarı Worker secret'ında kalır).
PUBLIC_REZERV_API_BASE="https://tcmb-rezerv-api.<hesap>.workers.dev"
